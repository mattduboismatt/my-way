
<div class="ui inverted menu" id = 'nav-menu'>
  <div class="right menu">

    <li class='item'><%= temp %>&deg</li>

    <a href="#" class='item' id="formula">Formula</a>

    <div id='formula-pop-up' class="ui warning message hide-me">
      <i class="close icon"></i>
      <div class="header">About our formula</div>
      <p>We use current weather, travel duration, cost, and distance to determine the your best mode.</p>
    </div>

    <%if current_user%>
      <%= link_to "Logout", logout_path, method: :delete, class: "item"%>
      <%= link_to "Profile", user_path(current_user), class: "item"%>
    <% else %>
      <%= link_to "Login", login_path, class: "item"%>
      <%= link_to "Register", new_user_path, class: "item"%>
    <% end %>

  </div>
  <div class="left menu">
    <%=link_to image_tag(select_image), root_path %>
  </div>
</div>






